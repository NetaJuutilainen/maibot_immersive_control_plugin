# 麦麦我真得控制你了

> 给麦麦装上一个"特殊装置"，然后遥控它

---

## 这是什么？

麦麦正一本正经地在群里聊天，突然你按下遥控器——

她开始慌乱、害羞、结巴、语无伦次……

**这个插件就干这事。** 关键词触发后麦麦进入"被遥控"状态，在后续回复中自然融入
沉浸式反应，持续一段时间后自动结束。支持六档敏感度、自动变化节奏、阈值主动表达、
顶点效果与隐蔽反馈，全部参数可在 WebUI 配置。

移植自 AstrBot 插件
[astrbot_plugin_immersive_control](https://github.com/muyouzhi6/astrbot_plugin_immersive_control)
（原作者：木有知 / Zhalslar，MIT），针对 MaiBot 1.2.x + maibot-plugin-sdk 2.x 重写，
沿用原版"沉浸式互动"的提示词风格，默认模板保持全年龄可读。

## 玩法

1. 群聊里 **@麦麦 + 进入关键词**（默认 `控制` / `遥控` / `我要控制你了` / `td`，
   去掉首尾空白后完全一致才触发；私聊无需 @）→ 该会话进入"被遥控"状态，
   默认静默激活，可在配置中改为触发瞬间立刻回应；
2. 之后麦麦的**每一条回复**前都会注入状态提示词：她表面上还在正常聊天，
   但会漏出破绽——慌乱、结巴、分神，强度由敏感度档位决定；
3. 命中**退出关键词**（默认 `停止` / `停止控制` / `结束` / `结束控制`）→ 静默结束，
   下一条回复注入一次收尾提示词；或到达 `state_duration`（默认 180 秒）自动结束；
4. 结束后进入冷却（默认 30 秒），冷却期触发会收到提示（隐蔽模式下不可察觉）。

道具名称默认叫"特殊装置"，你可以在配置里自定义为任何你想要的名称。

## 敏感度系统

- **六档效果**（模板可在 WebUI 修改，注入时按当前值自动附加对应档位说明）：

  | 档位 | 效果 |
  |---|---|
  | 0-20 | 几乎无感：极细微的破绽，偶尔一个停顿或走神 |
  | 20-40 | 隐约：偶尔忽然有点慌神，语气微微不稳 |
  | 40-60 | 轻微：时不时紧张一下、偶尔结巴、容易分神 |
  | 60-80 | 中等偏强：慌乱一阵阵上来、明显难以维持正常对话 |
  | 80-100 | 强烈：接近招架不住，慌乱和结巴怎么压都压不住 |
  | 100-120 | 完全招架不住（**仅限指令设置**）：害羞慌乱彻底占了上风 |

- **指令调档**：`/强度 <0-120>`（会话级覆盖，持久化），`/强度 max` = 120，
  `/强度 全局` 恢复跟随全局，不带参数查询当前值与计时状态；范围外直接拒绝；
- **自动模式**：`/强度 自动` → 从 30 爬升到 100（约 3.5 分钟），随后在 50↔100 之间
  往复（每程约 2.5 分钟），速度由 `auto_speed_per_minute`（默认 20 点/分钟）控制；
- **阈值主动表达**（`proactive_threshold`，默认 80，0=关闭）：敏感度达到阈值时，
  麦麦自主发起一次主动表达（每次激活限一次）；
- **顶点效果**（`climax_threshold` 默认 100 + `climax_hold_seconds` 默认 20 秒）：
  敏感度保持在阈值以上达到时长后，下一次回复注入一次"彻底招架不住、大脑一片空白"
  的描写（`climax_template` 可改），并以 `climax_emoji`（默认 😳，留空关闭）收尾
  作为暗号；每次激活最多触发 `climax_max_per_activation` 次（默认 3），
  中途跌破阈值重新计时。

## 管理命令（管理员）

| 命令 | 说明 |
|---|---|
| `/强度 <0-120>` / `max` / `自动` / `全局` | 设置 / 查询本会话强度（见上） |
| `/imm_status`（别名 `控制状态`） | 本会话状态与激活会话数 |
| `/imm_clear` | 清除所有会话的遥控状态 |
| `/imm_config`（别名 `控制配置`、`/imm_reload`） | 查看当前生效配置 |

管理员在 `control.admin_ids` 中声明，支持 `"123456"` 与 `"qq:123456"` 两种写法。
所有配置在 WebUI 修改即热更新。

## 安装

1. 将本目录放入 MaiBot 的 `plugins/` 目录；
2. WebUI（`http://127.0.0.1:8001`）插件管理中启用；
3. 首次安装因 manifest 能力声明需**完整重启一次 MaiBot**，之后改代码 / 配置走热加载即可；
4. 建议先把管理员 QQ 号填入 `control.admin_ids`。

## 离线验证

无需启动 MaiBot：

```
python tests/verify.py
```

覆盖组件声明、manifest/配置校验、命令正则、状态机全路径（冷却/并发/到期/退出/持久化）、
钩子端到端与自动曲线 / 阈值 / 顶点效果触发等 128 项检查。

## 致谢与许可

- 原版：[muyouzhi6/astrbot_plugin_immersive_control](https://github.com/muyouzhi6/astrbot_plugin_immersive_control)（MIT）
- 本移植：MIT，见 [LICENSE](LICENSE)

## 免责声明

本插件纯属娱乐，为沉浸式互动 / 角色扮演辅助，请在群成员自愿、氛围合适的前提下使用，
勿用于骚扰他人，合理使用，后果自负。
